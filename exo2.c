#include <stdio.h>
#include <stdlib.h>
int main() {
    int n ,i;
    printf("Enter tow numbers n and i : ");
    scanf("%d %d", &n, &i);
  printf("the sum of the tow numbers is  : %d",n+i);
    return 0;
}